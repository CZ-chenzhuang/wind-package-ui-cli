{
  "name": "root",
  "private": true,
  "workspaces":[
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "build": "lerna exec --scope wind-test -- yarn build",
    "start": "lerna exec --scope wind-test -- yarn start",
    "pub": "lerna exec --scope wind-ui-cli -- yarn pub",
    "cli:link": "lerna exec --scope wind-ui-cli -- yarn link"
  }
}
